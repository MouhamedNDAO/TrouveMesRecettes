import { Text, TouchableOpacity } from "react-native";

const CustomButtons = () => {
    
    return (
      <TouchableOpacity className ={'bg-secondary rounded-xl item-center justify-left  h-full mb-10'}>
            <Text>Mes Favoris</Text>
      </TouchableOpacity>
    );
  };
  
  
  export default CustomButtons;